# Fonte de dados provisionada: o Grafana sobe já apontando para o Prometheus.
# O uid fixo é referenciado pelos painéis do dashboard versionado.
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
